package logic;

import java.util.regex.Matcher;
import java.util.regex.Pattern;

/**
 * <h1>The Class Validation.</h1>
 * Functions as a class, that controls if 
 * newly created users usernames' and password's 
 * contain the correct form.
 * 
 * @author Oscar Brügger
 * @version 2.8
 * @since 2014-04-01
 */
public class Validation {
	
	/**
	 * Declare a private static variable usrNamePtrn of type Pattern.
	 * The password has to contain at least one integer, one letter
	 * and has to be between 6 and 100 characters long.
	 */
	
	
	/**
	 * Declare a private static variable usrNamePtrn of type Pattern.
	 * The username has to contain at least one @ and at least one '.',
	 * and has to be between 1 and 1001 characters long.
	 */
	

	
	/**
	 * My password validate method.
	 *
	 * @param passWord 		Reads the password from the TextField and determines if 
	 * 						it contains declared characters.
	 * @return true, if successful
	 */
	
	
	/**
	 * My user name validate method.
	 *
	 * @param userName 		Reads the username from the TextField and determines if
	 * 						it contains declared characters.
	 * @return true, if successful
	 */

}
